spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gigigenie
    username: postgres
    password: 1005
  jpa:
    hibernate:
      ddl-auto: none
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

upstage:
  api:
    key: ${UPSTAGE_API_KEY}

openai:
  api:
    key: ${OPENAI_API_KEY}
